export default interface RouteSegment {
    segmentId: string,
    routeId: string,
    segmentType: string,
    startTime: number,
    endTime: number,
    startLocation: string,
    endLocation: string
}